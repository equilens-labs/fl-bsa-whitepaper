model_family: FL-BSA native/no-CTGAN
seed: 42
training_protocol:
  hyperparameter_search: not_applicable
  notes: >
    The rc6 intake certificates identify the first-party evidence-native backend.
    CTGAN/WGAN hyperparameters such as epochs, PAC, batch size, and GAN layer
    widths are not applicable for this run.
branches:
  amplification:
    chosen:
      algorithm: native_branch_profile_empirical_sampler
      backend_id: first_party_evidence_native
      hyperparameter_validation_passed: false
      disposition: first_party_backend_has_no_tunable_hyperparameters
    search_ranges: {}
  intrinsic:
    chosen:
      algorithm: native_branch_profile_empirical_sampler
      backend_id: first_party_evidence_native
      hyperparameter_validation_passed: false
      disposition: first_party_backend_has_no_tunable_hyperparameters
    search_ranges: {}
