\section{Commit-Pinned Implementation Reference}

This paper describes only product tag \ProductReleaseTag{} at commit
\IdentityText{\ProductCommitRaw}. The commit-pinned modules relevant to the displayed surfaces are:

\begin{itemize}
  \item \path{flbsa.synthetic.generators.fl_bsa_ng} for the selected evidence-native branch path;
  \item \path{flbsa.synthetic.fair_decisions} for the intrinsic post-label policy;
  \item \path{flbsa.orchestrator.tasks.audit} for the publication uncertainty export and
        highest-rate race comparator;
  \item \path{flbsa.metrics.fairness_uncertainty} for Wilson, AIR delta, SRG, p-value, and
        multiplicity surfaces; and
  \item certificate hashing and release-evidence workflow code at the same commit for provenance.
\end{itemize}

The product source also contains a separate configured-reference comparison helper. Its existence
is why this paper distinguishes configured white from effective black and does not claim one
uniform race method across every v5.0.1 output surface. Repository access is required for source
review; the companion permits offline verification of the evidence actually consumed by the paper.
