\section{Generator Configuration}
This appendix summarises the branch generator configuration recorded in the intake certificates for this run. The current native/no-CTGAN evidence path uses the first-party evidence-native generator; CTGAN-specific fields such as epochs, PAC, batch size, and GAN layer widths are therefore not applicable and are not reported as chosen hyperparameters.

\subsection{Branch Configuration}
\begin{table}[t]
\centering
\input{includes/table_hparams_chosen}
\caption{Generator configuration and hyperparameter disposition for each branch in this synthetic audit run, derived from the intake certificates when available.}
\label{tab:generator_config_branches}
\end{table}
