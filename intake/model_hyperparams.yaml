model_family: FL-BSA first-party evidence-native
seed: 42
training_protocol:
  hyperparameter_search: not_applicable
  notes: >
    The v5.0.1 intake certificates select the first-party evidence-native backend.
    The selected branch-profile sampler has no declared search dimensions for this run;
    applicable configuration is recorded in the branch certificates.
branches:
  amplification:
    chosen:
      algorithm: native_branch_profile_empirical_sampler
      backend_id: first_party_evidence_native
      hyperparameter_validation_passed: false
      disposition: first_party_backend_has_no_tunable_hyperparameters
    search_ranges: {}
  intrinsic:
    chosen:
      algorithm: native_branch_profile_empirical_sampler
      backend_id: first_party_evidence_native
      hyperparameter_validation_passed: false
      disposition: first_party_backend_has_no_tunable_hyperparameters
    search_ranges: {}
