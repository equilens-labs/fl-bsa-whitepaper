\section{Generator Configuration}
This appendix summarises the branch generator configuration recorded in the intake certificates for this run. The exact intake identifies the generator backend as \texttt{\ReleaseGeneratorBackend}. Fields that do not apply to that backend are not reported as chosen hyperparameters.

\subsection{Branch Configuration}
\begin{table}[htbp]
\centering
\input{release/includes/table_hparams_chosen}
\caption{Generator configuration and hyperparameter disposition for each branch in this synthetic audit run, derived from the intake certificates when available.}
\label{tab:generator_config_branches}
\end{table}
